<div class="container mx-auto px-6 py-10 max-w-2xl">

    <h1 class="text-2xl font-black text-slate-800 mb-8">👻 Edit Task</h1>

    <form method="POST" action="<?php echo WEB_ROOT; ?>/task/edit?id=<?= $this->task['id'] ?>">
        <div class="bg-gradient-to-r from-sky-400 via-sky-200 to-sky-50 rounded-2xl shadow-sm border border-slate-100 p-8 flex flex-col gap-6">

            <div class="flex flex-col gap-1.5">
                <label class="text-sm font-semibold text-slate-600" >Name</label>
                <input type="text" name="name" value="<?= $this->task['name'] ?>" class="border border-slate-200 rounded-xl px-4 py-2.5 text-sm focus:outline-none focus:border-sky-400" placeholder="Task name">
            </div>

            <div class="flex flex-col gap-1.5">
                <label class="text-sm font-semibold text-slate-600">Description</label>
                <textarea name="description" rows="3" class="border border-slate-200 rounded-xl px-4 py-2.5 text-sm focus:outline-none focus:border-sky-400"><?= $this->task['description'] ?></textarea>
            </div>

            <div class="flex gap-4">
                <div class="flex flex-col gap-1.5 flex-1">
                    <label class="text-sm font-semibold text-slate-600">Start Date</label>
                    <input type="date" name="start_date" value="<?= $this->task['startDate'] ?>" class="border border-slate-200 rounded-xl px-4 py-2.5 text-sm focus:outline-none focus:border-sky-400">
                </div>
                <div class="flex flex-col gap-1.5 flex-1">
                    <label class="text-sm font-semibold text-slate-600">Finish Date</label>
                    <input type="date" name="finish_date" value="<?=  $this->task['finishDate'] ?>" class="border border-slate-200 rounded-xl px-4 py-2.5 text-sm focus:outline-none focus:border-sky-400">
                </div>
            </div>

            <div class="flex flex-col gap-1.5">
                <label class="text-sm font-semibold text-slate-600">User</label>
                <input type="text" name="user" value="<?= $this->task['user'] ?>" class="border border-slate-200 rounded-xl px-4 py-2.5 text-sm focus:outline-none focus:border-sky-400" placeholder="Your name">
            </div>

            <div class="flex justify-end gap-3 mt-2">
                <a href="<?php echo WEB_ROOT; ?>/dashboard" class="px-6 py-2.5 rounded-xl text-sm font-semibold text-slate-500 border border-slate-200 hover:border-sky-300 transition-colors">Cancel</a>
                <button type="submit" class="px-6 py-2.5 rounded-xl text-sm font-semibold bg-sky-500 hover:bg-sky-200 text-white transition-colors">Edit Task</button>
            </div>

        </div>
    </form>

</div>